{
  "name": "nexus-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "wasm": "cd ../engine && wasm-pack build --target web --out-dir ../web/src/wasm",
    "dev": "vite",
    "test": "vitest run",
    "build": "tsc && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
