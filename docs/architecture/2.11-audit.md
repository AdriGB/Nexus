# Phase 2.11 architecture audit

- Issue: [#127](https://github.com/AdriGB/Nexus/issues/127)
- Scope: architecture only; no behavior changes
- Baseline: `main` at the start of Phase 2.11

## Executive summary

Nexus substantially follows ADR 0001: it is a modular monolith, the bridge is
transport-only, domain rules generally live below the simulation facade, and
the tick order is discoverable in `pipeline.rs`. No critical boundary violation
was found.

The main exception is orchestration duplication. `run_step`,
`run_profiled_step`, and `run_profiled_autonomy_step` encode overlapping tick
lifecycles, while `run_autonomy` and `profile_autonomy` separately encode the
entity loop. Their phase order has already drifted in small ways. This is the
first high-priority refactor because every benchmark and scheduler change would
otherwise have to preserve several implementations.

`Simulation` remains both the state container and the integration service for
several domains. This is acceptable during incremental extraction, but event
translation, relationship consequences, lifecycle integration, and household
storage are now distinct responsibility clusters. They should be extracted
only behind concrete outcome contracts, not merely to shorten the file.

## Audit method and severity

The audit inspected `simulation/mod.rs`, `simulation/pipeline.rs`, all autonomy
modules, `households.rs`, `events.rs`, `dependents.rs`, `kinship.rs`,
`lifecycle.rs`, and all bridge modules. It traced imports and visibility,
identified state mutation sites, compared execution paths, and noted structural
scans and allocations. It did not attempt to measure runtime cost.

- **Critical:** breaks determinism or ownership now; fix before other work.
- **High:** makes safe scale work unreliable or creates material rule coupling.
- **Medium:** a concrete boundary or scale risk requiring evidence before change.
- **Low:** surface-area or clarity debt with limited immediate risk.

## Domain inventory and state ownership

| Domain / layer | State owned | Commands / mutations | Queries / outputs | Direct dependencies |
| --- | --- | --- | --- | --- |
| Pipeline | Tick phase order; no persistent state | Runs lifecycle phases | Profiles | Simulation and domain operations |
| Simulation facade | Tick, seed, IDs, counters, entities, caches, histories and domain stores | Advance, spawn, transfer and integrate outcomes | Stats and read-only views | All simulation domains, world, pathfinding |
| Entity / physiology | Identity, body, activity and vital values | Advance needs and resolve starvation | Life state | Config, autonomy mind |
| Autonomy | Mind, memory, goal, plan and action state | Perceive, decide, plan, act and interact | Outcomes, discoveries and attempts | Entity, inventory, spatial, pathfinding, world |
| Inventory | Item quantities and capacity | Add, remove and transfer | Amount and remaining capacity | None |
| Households | Household identity, residence, storage and migration state | Form, migrate, dissolve, inherit and store | Membership and statistics | Entity, inventory, genealogy, world |
| Dependents | Caregiver link stored on `Entity` | Clear, reassign, feed and colocate dependents | Caregiver selection | Entity, autonomy goals, world |
| Lifecycle | Pregnancy and age state stored on `Entity` | Conception, pregnancy and birth outcomes | Eligibility | Entity, time, world |
| Genealogy / kinship | Parent links and genealogy index | Register parentage | Relatives and family trees | Entity / genealogy |
| Relationships / grief | Affinity and grief stored in `Mind`; partner ID on `Entity` | Affinity, partnership and grief transitions | Relationship role | Entity, autonomy memory, genealogy |
| Events | Recent event ring and next event ID | Append causal events | Iteration and entity summaries | World resource kinds |
| Spatial infrastructure | Population snapshots and grid buckets | Rebuild index | Nearby candidate indices | Entity snapshots, world dimensions |
| Bridge | DTO-local temporary values only | Serialize engine queries | JSON transport | Simulation facade and world |

## Required invariants

| Owner | Invariant |
| --- | --- |
| Simulation | Tick, entity IDs, event IDs and counters are monotonic |
| Simulation / entity | Entity IDs are unique and the collection remains sorted for binary search |
| Autonomy | Knowledge enters memory only through perception or explicit interaction |
| Inventory | Quantities and capacity arithmetic cannot underflow or exceed capacity |
| Households | Entity membership and household membership/storage access agree |
| Genealogy | Parentage is registered consistently and derived relations invent no links |
| Events | IDs are nonzero and causal references identify already-created events |
| Spatial | Snapshots and buckets describe entity positions at their consumption phase |
| World integration | Resource mutations advance `world_revision` consistently |
| Pipeline | Normal, phase-profiled and autonomy-profiled ticks produce equal logical state |

## Dependency map

```text
web UI
  -> WASM facade (`lib.rs`)
      -> bridge DTO/serialization modules
          -> simulation query facade + world queries

simulation pipeline
  -> Simulation state/integration facade
  -> physiology, dependents, households, grief
  -> lifecycle and relationship maintenance through Simulation helpers

Simulation integration facade
  -> autonomy -> perception / decision / action / social
              -> spatial + pathfinding + world
  -> households -> entity + inventory + genealogy + world
  -> events -> world resource kinds
  -> lifecycle -> entity + world
  -> kinship -> genealogy + entity
```

The bridge does not feed DTOs back into engine rules. Domain modules do not
depend on the bridge or web UI. The notable coupling is inside the simulation
boundary, where multiple domains mutate fields stored directly on `Entity` and
`Mind`; the integration facade currently enforces the cross-domain ordering.

## Findings

| ID | Finding | Severity | Area | Evidence | Recommended action |
| --- | --- | --- | --- | --- | --- |
| A01 | Normal and profiled tick orchestration are duplicated and have drifted | High | `pipeline.rs` | Three functions repeat the lifecycle; infant snapping occurs at different points and profiling groups unrelated work as `pregnancies_us` | Replace them with one tick executor and an optional instrumentation sink; retain parity tests |
| A02 | Normal and profiled autonomy loops are separate implementations | High | `simulation/mod.rs`, `autonomy/profiling.rs` | Both allocate the same outcome vectors, iterate eligible entities, run social processing and aggregate mutations | Make instrumentation a parameter of one autonomy loop after A01 |
| A03 | `Simulation` translates many domain outcomes and also decides relationship consequences | High | `simulation/mod.rs` | Food sharing records events, changes affinity and may dissolve partnerships in one facade method | Define an explicit food-sharing outcome/consequence contract; extract incrementally after pipeline unification |
| A04 | Lifecycle integration remains concentrated in `Simulation` | Medium | `simulation/mod.rs` | Death removal, death event creation, births, household assignment and birth events share facade methods | Keep ordering in pipeline; move cohesive mutation rules behind lifecycle commands returning outcomes |
| A05 | Household state ownership is split across household records and entity fields | Medium | `households.rs`, `entity.rs`, `simulation/mod.rs` | Membership is stored on entities while household storage/migration is separate; callers pass both mutable collections | Add invariant tests first; introduce a narrow aggregate command API only if it reduces mutation sites |
| A06 | Household contexts allocate a population-sized temporary vector each autonomy tick | Medium | `Simulation::run_autonomy`, `run_profiled_autonomy_step` | `entities.iter().map(...).collect()` builds `Vec<Option<HouseholdAutonomyContext>>` | Instrument allocations and benchmark before choosing caching, lookup-on-demand, or batch construction |
| A07 | Autonomy returns a large positional tuple and several temporary vectors | Medium | `AutonomyRunResult`, `run_autonomy`, `profile_autonomy` | Consumption, discoveries, encounters and three attempt classes travel as tuple positions | Replace with a named `AutonomyTickOutcome`; measure reuse/capacity separately |
| A08 | Several maintenance operations perform global scans and rebuild sets/vectors | Medium | `dependents.rs`, `households.rs`, `lifecycle.rs` | Caregiver reassignment scans candidates; household settlement builds `HashSet`s and candidate vectors; conception builds candidates | Record cadence and counters in benchmarks before event-driven or indexed replacements |
| A09 | Event summaries scan the recent ring per query | Medium | `events.rs` | Entity summary filters the full bounded event iterator | Measure bridge query frequency and history capacity; add an index only if the query becomes material |
| A10 | Kinship queries allocate complete result collections repeatedly | Medium | `kinship.rs` | Children, siblings, ancestors, descendants and family trees return new vectors/sets | Benchmark inspector and future rule-engine workloads before caching derived relations |
| A11 | Autonomy exposes more symbols through its facade than a stable domain contract needs | Low | `autonomy/mod.rs` | Mixed `pub`, `pub(crate)`, `pub(super)` and test-only re-exports expose implementation-stage functions/types | Audit consumers after A01/A02 and narrow visibility; do not churn APIs beforehand |
| A12 | Bridge modules receive broad engine objects for some queries | Low | `bridge/kinship.rs`, `bridge/households.rs` | Serialization functions accept `&Simulation`, while entity DTOs accept focused values | Prefer focused query results if this improves testability; keep domain logic out of bridge |
| A13 | Phase profile names do not match all work they measure | Medium | `pipeline.rs`, `PhaseProfile` | `pregnancies_us` includes grief, orphan reassignment, household synchronization, dissolution and inheritance | Define stable phase names as part of unified instrumentation, without preserving misleading labels as architecture |
| A14 | No enforceable architectural test currently protects dependency direction | Low | crate/module structure | Boundaries are documented and visibility helps, but dependency direction is not mechanically checked | Add lightweight source/import or compile-fail checks only if they remain cheaper than review |

No critical finding was recorded. Medium scale findings are hypotheses to
measure, not authorization to optimize.

## Responsibilities retained by `Simulation`

The following clusters are candidates for gradual extraction:

1. **Autonomy integration:** building household contexts, collecting outcomes,
   feeding dependents, and translating autonomy outcomes into domain actions.
2. **Relationship consequences:** interaction events, affinity events, food
   sharing consequences, partnership formation and dissolution.
3. **Lifecycle integration:** death collection, birth materialization, genealogy
   registration, household assignment and causal event emission.
4. **Household storage facade:** access validation followed by inventory/storage
   transfer across two owners.
5. **Event translation:** repeated construction of central event envelopes.

These responsibilities should not all move into `events` or `pipeline`.
Pipeline owns order, domains own rule meaning, and the composition root may keep
translation between stable domain outcomes and the central event stream.

## Public surface review

The public API visible outside `simulation` is mostly intentional: bridge and
WASM code require entity views, profiles, statistics and simulation commands.
The highest-value visibility review is internal to `autonomy`, where stage
implementation types are re-exported at several visibility levels. Closing
them now would create noise before the duplicated execution paths are unified.

Recommended rule: after every extraction, run `rg` over each changed export,
narrow it to its actual consumers, and keep test helpers test-only. Do not add a
generic domain API merely to replace a concrete `pub(crate)` item.

## Prioritized refactor queue

1. **High — one tick pipeline:** execute one ordered lifecycle with optional
   instrumentation; keep normal/profiled parity coverage.
2. **High — one autonomy loop:** route sampled timing through the same entity
   update and outcome aggregation path.
3. **Medium — stable instrumentation phases:** correct phase names, add counters
   and establish reproducible scenario output.
4. **Medium — named domain outcomes:** replace positional autonomy tuples and
   isolate relationship/lifecycle consequence contracts.
5. **Medium — invariant coverage:** add focused tests for entity ordering,
   household membership, event IDs and spatial snapshot timing.
6. **Medium — benchmark structural risks:** quantify household contexts, global
   maintenance scans, kinship allocations and event-summary queries.
7. **Low — visibility sweep:** narrow internal exports based on observed callers.
8. **Conditional — architectural enforcement:** add dependency tests only where
   review repeatedly fails to protect a boundary.

## Exit criteria

- [x] Inventory current domains
- [x] Record state ownership and invariants
- [x] Map dependencies between domains
- [x] Identify misplaced responsibilities in `Simulation`
- [x] Identify duplicated orchestration
- [x] Review unnecessarily broad internal APIs
- [x] Identify structural scale risks without optimizing them
- [x] Classify findings as critical / high / medium / low
- [x] Produce a prioritized refactor queue
- [x] Make no behavior changes
- [x] Run `./scripts/check.ps1` successfully

The next issue should implement A01 only: one instrumentable tick pipeline. It
must not bundle benchmark infrastructure, scale optimizations, or unrelated
domain extraction.
